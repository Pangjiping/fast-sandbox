apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fast-sandbox-fastpath-ingress
  namespace: fast-sandbox-system
spec:
  podSelector:
    matchLabels:
      app: fast-sandbox-fastpath
      fast-sandbox.io/control-plane-role: fastpath
  policyTypes: [Ingress]
  ingress:
  - from:
    - podSelector:
        matchLabels:
          fast-sandbox.io/control-plane-client: "true"
    ports:
    - {protocol: TCP, port: 9090}
  - from:
    - podSelector:
        matchLabels:
          fast-sandbox.io/metrics-client: "true"
    ports:
    - {protocol: TCP, port: 9091}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fast-sandbox-proxy-ingress
  namespace: fast-sandbox-system
spec:
  podSelector:
    matchLabels:
      app: fast-sandbox-proxy
  policyTypes: [Ingress]
  ingress:
  - from:
    - podSelector:
        matchLabels:
          fast-sandbox.io/data-plane-client: "true"
    ports:
    - {protocol: TCP, port: 8080}
  - from:
    - podSelector:
        matchLabels:
          fast-sandbox.io/metrics-client: "true"
    ports:
    - {protocol: TCP, port: 9094}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fast-sandbox-controller-ingress
  namespace: fast-sandbox-system
spec:
  podSelector:
    matchLabels:
      app: fast-sandbox-controller
      fast-sandbox.io/control-plane-role: controller
  policyTypes: [Ingress]
  ingress:
  - from:
    - podSelector:
        matchLabels:
          fast-sandbox.io/metrics-client: "true"
    ports:
    - {protocol: TCP, port: 9091}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sandbox-fastlet-ingress
  namespace: fast-sandbox
spec:
  podSelector:
    matchLabels:
      app: sandbox-fastlet
  policyTypes: [Ingress]
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          sandbox.fast.io/scope: system
      podSelector:
        matchExpressions:
        - key: fast-sandbox.io/control-plane-role
          operator: In
          values: [controller, fastpath]
    ports:
    - {protocol: TCP, port: 5758}
  - from:
    - namespaceSelector:
        matchLabels:
          sandbox.fast.io/scope: system
      podSelector:
        matchLabels:
          app: fast-sandbox-proxy
    ports:
    - {protocol: TCP, port: 5780}
  - from:
    - namespaceSelector:
        matchLabels:
          sandbox.fast.io/scope: system
      podSelector:
        matchLabels:
          fast-sandbox.io/metrics-client: "true"
    ports:
    - {protocol: TCP, port: 9093}
